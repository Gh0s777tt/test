[package]
name = "vantis-cytadela"
version = "0.1.0"
edition = "2021"
authors = ["VANTIS OS Team"]
description = "Cytadela app sandbox and package runtime crate for VantisOS"
license = "MIT"

[lib]
path = "mod.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
